import React from "react"
import Layout from "../components/layout-default"

const NotFoundPage = () => (
  <Layout>
    <h1>NOT FOUND</h1>
    <p>Page not found.</p>
  </Layout>
)

export default NotFoundPage
